import React from 'react';
import PageWrapper from '../PageWrapper';

const SearchMessagePage = () => (
	<PageWrapper className="search-message">
		<p>
			Type in the search field and press enter to search
		</p>
	</PageWrapper>
);

export default SearchMessagePage;
